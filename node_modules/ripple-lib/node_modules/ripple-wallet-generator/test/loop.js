var rippleLib = require('ripple-lib');

var Wallet = require(__dirname+'/../lib/wallet.js')({ sjcl: rippleLib.sjcl });;

var wallet;

while (true) {
  wallet = Wallet.generate();
  console.log(wallet);
}

